<UserControl x:Class="PhotoCull.Views.PhotoGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PhotoCull.Converters"
             xmlns:helpers="clr-namespace:PhotoCull.Helpers"
             Background="Transparent">
    <UserControl.Resources>
        <conv:CullStatusToColorConverter x:Key="StatusColor"/>
        <conv:RatingToStarsConverter x:Key="StarConv"/>
    </UserControl.Resources>

    <!-- Use ListBox with custom VirtualizingUniformGrid for true virtualization -->
    <ListBox x:Name="PhotoItems"
             Background="Transparent" BorderThickness="0"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             ScrollViewer.CanContentScroll="True"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling"
             SelectionMode="Extended"
             SelectionChanged="OnSelectionChanged">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <helpers:VirtualizingUniformGrid x:Name="VirtGrid"
                    ItemWidth="{Binding ThumbnailCellWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    ItemHeight="{Binding ThumbnailCellHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border x:Name="PhotoBorder" Margin="4" Padding="2"
                        Background="#FF2D2D2D" CornerRadius="4"
                        BorderThickness="2" BorderBrush="Transparent"
                        MouseLeftButtonDown="OnPhotoClick"
                        MouseRightButtonDown="OnPhotoRightClick"
                        Tag="{Binding Id}">
                    <DockPanel>
                        <!-- Image area - adapts to landscape/portrait automatically via Stretch="Uniform" -->
                        <Image x:Name="ThumbImage"
                               DockPanel.Dock="Top"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="LowQuality"
                               Loaded="OnThumbImageLoaded"
                               Tag="{Binding}"/>
                        <!-- Info bar -->
                        <Grid DockPanel.Dock="Bottom" Margin="4,4,4,2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding FileName}" FontSize="10" Foreground="#FFAAAAAA"
                                       TextTrimming="CharacterEllipsis"/>
                            <Ellipse Grid.Row="0" Grid.Column="1" Width="8" Height="8" Margin="4,0,0,0"
                                     Fill="{Binding Status, Converter={StaticResource StatusColor}}"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                       Text="{Binding Rating, Converter={StaticResource StarConv}}"
                                       FontSize="10" Foreground="#FFFFC107"/>
                        </Grid>
                    </DockPanel>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
