<UserControl x:Class="PhotoCull.Views.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PhotoCull.Converters"
             Background="#FF111111">
    <UserControl.Resources>
        <conv:ByteArrayToImageSourceConverter x:Key="ImgConv"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8" HorizontalAlignment="Right">
            <Button Content="关闭对比" Click="OnCloseClick" Padding="12,6"
                    Background="#FF3D3D3D" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- Photo Grid -->
        <ItemsControl Grid.Row="1" x:Name="CompareItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="4" Padding="4" CornerRadius="4"
                            BorderThickness="3" BorderBrush="Transparent"
                            Background="#FF1E1E1E"
                            MouseLeftButtonDown="OnComparePhotoClick"
                            Tag="{Binding Id}">
                        <StackPanel>
                            <Image Source="{Binding ThumbnailData, Converter={StaticResource ImgConv}}"
                                   Stretch="Uniform" MaxHeight="400"/>
                            <TextBlock Text="{Binding FileName}" FontSize="11" Foreground="#FFAAAAAA"
                                       HorizontalAlignment="Center" Margin="0,4"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
